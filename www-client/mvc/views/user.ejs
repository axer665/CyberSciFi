<!DOCTYPE html>
<html>
<head>
    <title>Список пользователей</title>
    <meta charset="utf-8" />
</head>
<body>
<h3>User information: </h3>

<p><b>ID:</b> <%= user._id %>></p>
<p><b>Login:</b> <%= user.login %></p>

<div class="populations">
    <h4>Populations: </h4>

    <div id="populations_list">
        <% if (typeof populations !== "undefined") { %>
            <% if (populations.length > 0) { %>
                <% for(var i=0; i<populations.length; i++) {%>
                    <p><%= populations[i].name %></p>
                <% } %>
            <% } else { %>
                    <p> no populations </p>
            <% } %>
        <% } %>
    </div>

    <a id="form_vision">Create population</a>
    <form id="form-population" action="" method="POST">
        <input type="text" class="form-population-name" name="name" />
        <select id="raceList">
            <option value="0" selected disabled> Select race </option>
        </select>
        <button type="submit">Create</button>
    </form>

</div>

</body>
<html>

<style>
    #form-population {
        display: none;
    }
</style>

<script src="/dist/user.bundle.js"></script>
